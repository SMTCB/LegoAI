{
    "name": "lego-ai-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "echo 'Deploy via Vercel'",
        "build": "npm install && cd web-app && npm install && npm run build"
    },
    "dependencies": {
        "@google/generative-ai": "^0.21.0",
        "axios": "^1.13.5",
        "body-parser": "^2.2.2",
        "cors": "^2.8.6",
        "dotenv": "^17.2.4",
        "express": "^5.2.1",
        "groq-sdk": "^0.3.3"
    }
}
